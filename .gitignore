# Go build and test output
/build/
/sgn
/sgn.exe
*.test
*.out

# Rust build output
/target/
**/*.rs.bk
*.pdb

# Cargo.lock pins the Rust dependency graph and pkg/sgn.wasm is the tracked
# module embedded by the Go package. Do not ignore either artifact.
!Cargo.lock
!pkg/sgn.wasm
